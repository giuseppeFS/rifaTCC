<nav id="navbar" class="navbar navbar-expand-lg navbar-light fixed-top bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">
      <img src="/assets/rifa_amiga_logo.png" alt="" width="50" height="50">
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
     <ul class="navbar-nav mb-2 mb-lg-0 ms-auto">
       <% if user_signed_in? %>
       <li class="nav-item"><%= link_to 'Sair', destroy_admin_session_path, method: :delete, class: "nav-link active" %></li>
       <% elsif institution_signed_in? %>
       <li class="nav-item"><%= link_to 'Sair', destroy_institution_session_path, method: :delete, class: "nav-link active" %></li>
       <% else %>
       <li class="nav-item"><%= link_to 'Entrar', sign_in_path, method: :get, class: "nav-link active" %></li>
       <% end %>
     </ul>
   </div>
 </div>
</nav>