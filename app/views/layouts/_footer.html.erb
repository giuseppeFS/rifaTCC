<div class="row">
	<div class="col-md-12">
			<%= debug(controller_path) if Rails.env.development? %>
			<%= debug(params) if Rails.env.development? %>
			<%= debug(pundit_user.class) if Rails.env.development? %>
	</div>
</div>